#include <bits/stdc++.h>
#define ll long long
using namespace std;
const int N = 1e7 + 9;
int n, m, k;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
#ifdef kimi
    freopen("../in.txt", "r", stdin);
    freopen("../out.txt", "w", stdout);
#endif
    cin >> n >> m >> k;
    for (int i = 1; i <= n; i++) {
    	for (int j = 1; j)
    }
    return 0;
}